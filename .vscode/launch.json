{
  "version": "0.2.0",
  "configurations": [{
    "name": "Executar extensão",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
  }]
}
